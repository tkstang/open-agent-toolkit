---
oat_generated: true
oat_generated_at: 2026-02-21
oat_review_scope: plan
oat_review_type: artifact
oat_project: .oat/projects/shared/rename-full-to-spec-driven-workflow
---

# Artifact Review: plan.md

**Reviewed:** 2026-02-21
**Scope:** plan.md artifact (quick-mode project)
**Files reviewed:** 2 (plan.md, discovery.md)
**Upstream artifacts used:** discovery.md, implementation.md, state.md

## Summary

The plan is well-structured with clear RED/GREEN/REFACTOR steps, correct file references for the core CLI and skill changes, and good use of grep-based refactor sweeps. However, it has two critical coverage gaps: (1) several `.oat/repo/reference/` documentation files and the sync manifest contain `promote-full` and `full|quick|import` mode references that are not covered by any task, and (2) verification grep commands in p02-t02 use an incorrect file glob pattern that would match zero files at runtime. There are also minor issues with the Reviews table listing spec/design reviews that are not applicable in quick mode.

## Findings

### Critical

- **Missing files: `.oat/repo/reference/` docs and `.oat/sync/manifest.json` not covered by any task** (plan.md, all phases)
  - Issue: A comprehensive grep sweep reveals that the following files contain workflow-mode references to `full` (as `promote-full`, `full|quick|import`, `full mode`, etc.) but are not listed in any plan task:
    - `.oat/repo/reference/decision-record.md` (lines 205, 211, 215, 225, 227, 228, 345, 370)
    - `.oat/repo/reference/roadmap.md` (lines 47, 221, 222, 227)
    - `.oat/repo/reference/current-state.md` (line 33)
    - `.oat/repo/reference/deferred-phases.md` (lines 20, 93)
    - `.oat/repo/reference/backlog-completed.md` (lines 131, 293)
    - `.oat/repo/knowledge/project-index.md` (lines 81, 118, 157)
    - `.oat/sync/manifest.json` (lines 266-267, 586-587) - contains `oat-project-promote-full` canonical/provider paths
  - These are active reference documents (not archived projects) that will contain stale `promote-full` and `full` mode references after the rename, directly violating discovery success criterion: "Documentation consistently describes three lanes: Spec-Driven, Quick, Import."
  - Fix: Add a new task (e.g., p02-t03) to update `.oat/repo/reference/` docs, `.oat/repo/knowledge/project-index.md`, and `.oat/sync/manifest.json`. Include a grep sweep of `.oat/repo` and `.oat/sync` in the refactor step. Note: `.oat/sync/manifest.json` will likely be regenerated by `oat sync` after the skill rename in p01-t02, so it may only need a verification check rather than manual edit.

- **Incorrect grep glob in p02-t02 verification commands** (plan.md:197-198)
  - Issue: The RED and GREEN step grep commands use `.agents/skills/oat-project-*.md` as a path argument. However, skill files are located at `.agents/skills/oat-project-*/SKILL.md` (inside subdirectories), not as top-level `.md` files. The shell glob `oat-project-*.md` will expand to zero matches because no files match that pattern in `.agents/skills/`. This means the verification step will silently report "no hits" even before any changes are made, making the RED step meaningless.
  - Fix: Change the path argument from `.agents/skills/oat-project-*.md` to `.agents/skills/oat-project-*/SKILL.md` (or simply `.agents/skills/` to let rg recurse). Apply the same fix to both the RED step (line 197) and the GREEN step (line 206).

### Important

- **p01-t02 refactor grep does not sweep `.oat/` directory** (plan.md:115-116)
  - Issue: The refactor grep `rg -n "oat-project-promote-full" .agents docs packages/cli` only searches `.agents`, `docs`, and `packages/cli`. It misses `.oat/sync/manifest.json`, `.oat/repo/reference/`, and `.oat/repo/knowledge/project-index.md` which all contain `oat-project-promote-full` references.
  - Fix: Expand the search path to include `.oat`: `rg -n "oat-project-promote-full" .agents docs packages/cli .oat/repo .oat/sync .oat/templates`

- **p01-t01 refactor grep incomplete for plan template references** (plan.md:69)
  - Issue: The refactor grep pattern `oat_workflow_mode:\s*full|oat_plan_source:\s*full|--mode full|\bfull mode\b` would catch the `oat_plan_source: full` in `.oat/templates/plan.md` line 9, but the grep paths `packages/cli .oat/templates` do not include `.oat/repo/reference/decision-record.md` which also has `oat_plan_source` references (line 228). Additionally, lines 164-165 of `.oat/templates/plan.md` say "required in full mode" which would be caught by `\bfull mode\b` but should be verified.
  - Fix: Expand the refactor grep to also include `.oat/repo` to catch decision record references.

- **p03-t02 uses `git add -A` which can stage unintended files** (plan.md:297)
  - Issue: The commit step uses `git add -A` which stages all changes in the working tree, including potentially unintended modifications. The project's AGENTS.md convention says "prefer adding specific files by name rather than using `git add -A`."
  - Fix: Replace with targeted staging, e.g., `git add .oat/ .agents/ packages/cli docs README.md` or whatever files were actually modified during the smoke check.

### Minor

- **Reviews table lists spec and design artifact reviews as pending** (plan.md:311-312)
  - Issue: The Reviews table includes rows for `spec` artifact review and `design` artifact review with `pending` status, but this is a quick-mode project where spec and design artifacts are not required and do not exist. These rows are misleading.
  - Suggestion: Remove the `spec` and `design` rows from the Reviews table, or mark them as `n/a (quick mode)`.

- **p02-t01 includes `docs/oat/workflow/pr-flow.md` but grep pattern may not fully cover it** (plan.md:140, 150-151)
  - Issue: The RED-step grep pattern `\bFull workflow lane\b|\bfull lifecycle\b|--mode full|workflow mode metadata \(`full`` is quite specific. The actual content in `docs/oat/workflow/pr-flow.md` (line 13) says `Required (full mode):` which would only be caught by `\bfull mode\b` if that pattern were included. The current pattern does not include `\bfull mode\b`.
  - Suggestion: Add `\bfull mode\b` to the p02-t01 RED-step grep pattern to ensure it catches all doc references.

- **`docs/oat/ideas/index.md` uses "full project lifecycle" phrasing** (not in plan)
  - Issue: `docs/oat/ideas/index.md` line 3 says "alternative to the full project lifecycle" and "promote them to full projects." While these use "full" as a natural-language adjective (meaning "complete"), they could cause confusion after the rename. The discovery constraint says to keep changes "focused on workflow-mode terminology, not unrelated uses of the word 'full'", so this is likely intentionally out of scope.
  - Suggestion: Consider whether the phrasing should be updated to "spec-driven project lifecycle" for consistency, or leave as-is per the constraint. No action required unless the implementer judges it confusing.

- **`commands.integration.test.ts` listed in p03-t01 may not need changes** (plan.md:234)
  - Issue: The file is listed in p03-t01's file list, but the only `full` reference is a test name `'init -> status -> sync -> status: full workflow'` (line 166) which uses "full" to mean "complete/end-to-end", not as a workflow mode value. Including it may lead to unnecessary churn.
  - Suggestion: Verify whether this file actually needs modification. If the test name is not related to workflow mode naming, it can be removed from the task's file list.

## Artifact Quality Assessment

### Completeness

The plan covers the core rename across CLI runtime, templates, skills, docs, and tests. Phase sequencing is logical: canonical contract first, docs/skills second, test alignment third. Each task has clear RED/GREEN/REFACTOR steps with verification commands. However, the `.oat/repo/reference/` documentation layer is a significant gap (see Critical finding #1).

### Internal Consistency

- Commit convention is consistent (`{type}({scope}): {description}`) across all tasks.
- Phase boundaries are logical and non-overlapping.
- The frontmatter correctly identifies `oat_plan_source: quick` and `oat_plan_hill_phases: []`.

### Alignment with Discovery

| Discovery Requirement | Plan Coverage | Status |
|----------------------|---------------|--------|
| `oat_workflow_mode` and `oat_plan_source` use `spec-driven` | p01-t01 covers templates and CLI runtime | covered |
| CLI scaffolding/help uses `spec-driven\|quick\|import` | p01-t01 + p03-t01 (help snapshots) | covered |
| Skill and reviewer contracts refer to Spec-Driven mode | p02-t02 covers skills and reviewer | covered |
| Documentation consistently describes three lanes | p02-t01 covers docs + README | partial - missing `.oat/repo/reference/` docs |
| Tests and snapshots pass with renamed mode | p03-t01 + p03-t02 | covered |
| No backward compatibility for legacy `full` | Plan explicitly does not add aliases | covered |
| Promotion path renamed | p01-t02 renames skill | covered |

| Discovery Risk | Plan Mitigation | Status |
|---------------|----------------|--------|
| Missed literals cause contract drift | Grep sweeps in refactor steps | partial - sweeps don't cover all directories |
| Partial skill rename breaks workflow install | p01-t02 bundles rename + registry update | covered |

### Actionability

Tasks have clear file lists, verification commands, and commit messages. The RED/GREEN/REFACTOR structure is well-applied. However, two verification commands have correctness issues (Critical #2, Important #1) that would undermine their effectiveness at catching regressions.

## Verification Commands

Run these to verify the plan's file coverage is complete:

```bash
# Find all files with workflow-mode "full" references (excluding archived projects and .git)
rg -n "promote-full|oat_workflow_mode.*full|oat_plan_source.*full|full\|quick\|import|\bfull mode\b" \
  --glob '!.oat/projects/archived/**' \
  --glob '!.git/**' \
  --glob '!.oat/projects/shared/rename-full-to-spec-driven-workflow/**' \
  /Users/thomas.stang/Code/open-agent-toolkit

# Verify the p02-t02 grep glob actually resolves to files
ls /Users/thomas.stang/Code/open-agent-toolkit/.agents/skills/oat-project-*.md 2>/dev/null; echo "Exit: $?"
ls /Users/thomas.stang/Code/open-agent-toolkit/.agents/skills/oat-project-*/SKILL.md 2>/dev/null; echo "Exit: $?"
```

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert findings into plan tasks.
